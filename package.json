{
  "name": "washi-ui-monorepo",
  "version": "0.0.0",
  "description": "HTML commenting engine monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @washi-ui/core dev",
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r --filter './packages/*' test",
    "lint": "pnpm -r --filter './packages/*' lint",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "dependencies": {
    "@types/node": "^25.0.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "jsdom": "^27.4.0"
  }
}
